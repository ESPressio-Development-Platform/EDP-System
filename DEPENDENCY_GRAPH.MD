# EDP-System Dependency Graph

> Revalidated: 2026-09-21 — Composition Attributes, qualified provider resolution, cross-domain Architecture validation, and the C++20 baseline introduce no ESPressio-library dependency.

## Library Dependency Tree

```text
EDP-System
└── no ESPressio library dependencies
```

## Dependency Classification

`EDP-System` is the root foundation for the currently implemented EDP libraries and has no mandatory or optional ESPressio library dependencies.

The C++20 standard library/toolchain is a build/runtime facility rather than an ESPressio library dependency. The Composition Framework's structural compile-time strings and Attribute resolution do not introduce an ESPressio dependency.
